//external imports
import React, { Component } from 'react';
import {
    Link
  } from 'react-router-dom';

class Products extends Component {
  render() {
    return (
        <div className="container-fluid">
            product stuff would go here
        </div>
    );
  }
}

export default Products;
